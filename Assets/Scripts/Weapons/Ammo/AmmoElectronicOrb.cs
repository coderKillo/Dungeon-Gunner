using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AmmoElectronicOrb : MonoBehaviour
{
    [SerializeField] private List<GameObject> _connectedOrbs;

    // private void OnDrawGizmos()
    // {
    //     foreach (var orb in _connectedOrbs)
    //     {
    //         Gizmos.DrawLine(transform.position, orb.transform.position);

    //     }
    // }
}
